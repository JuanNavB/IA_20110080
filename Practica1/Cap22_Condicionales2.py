color = "rojo" #se decalara la variable color


if color != "rojo": #se analiza la variable rojo
    print("El color no es rojo." )#se imprime el resultado

else :#se evalua la ecepcion
    print( "El color es rojo.")#se imprime el resultado de la ecepcion